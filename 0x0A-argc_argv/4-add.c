#include <stdio.h>

/**
 * main - function that prints the sum ob positive numbers
 * @argc: argument count
 * @argv: an array of the argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	unsigned int num, addNum;

	addNum += num;

	if (argc == 1)
		printf("%d\n", "0");

	else if (argv[num] > '9' || argv[num] < '0')
	{
		printf("Error\n");
		return (1);
	}
