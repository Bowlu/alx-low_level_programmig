#include "main.h"
/**
 * _islower-> checks the lowercase characters
 * @c: is a character parameter
 * Description: print a function that checks the lowercase characters
 * Return: 1 if true and 0 if false
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
