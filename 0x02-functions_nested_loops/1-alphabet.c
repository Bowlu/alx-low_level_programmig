#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print all the alphabets in lowercase
 *
 * Return: 0
 */
void print_alphabet(int ch) 
{
	int character = ch;
}
int main(void)
{
	print_alphabet;
	int ch = 'a';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
